apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "ror-app.fullname" . }}-env
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ror-app.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}

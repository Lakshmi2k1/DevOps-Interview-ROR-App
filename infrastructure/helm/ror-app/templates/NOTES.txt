{{ .Chart.Name }} has been deployed to namespace "{{ .Release.Namespace }}".

1. Watch the pods come up:
   kubectl get pods -n {{ .Release.Namespace }} -w

{{- if .Values.ingress.enabled }}

2. Get the public ALB hostname (may take 1-3 minutes to be provisioned):
   kubectl get ingress {{ include "ror-app.fullname" . }} -n {{ .Release.Namespace }} \
     -o jsonpath='{.status.loadBalancer.ingress[0].hostname}'

3. Open http://<that-hostname>/ in your browser.
{{- end }}
